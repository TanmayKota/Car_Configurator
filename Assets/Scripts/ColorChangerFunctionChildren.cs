using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColorChangerFunctionChildren : MonoBehaviour
{
    public GameObject body1;
    public GameObject body2;
    public GameObject body3;
    public GameObject body4;
    public GameObject body5;
    public GameObject body6;
    public GameObject body7;
    public GameObject body8;
    public GameObject body9;
    public GameObject body10;
    public GameObject body11;
    public GameObject body12;
    public GameObject body13;
    public GameObject body14;
    public GameObject body15;
    public GameObject body16;
    public GameObject body17;
    public GameObject body18;
    public GameObject body19;
    public GameObject body20;
    public GameObject body21;
    public GameObject body22;
    public GameObject body23;
    public GameObject body24;
    public GameObject body25;
    public GameObject body26;
    public GameObject body27;
    public GameObject body28;
    public GameObject body29;
    public GameObject body30;

    public void ColorChangerFunction(Material material)
    {
        Debug.Log("Button Click");

        if (body1 != null && body1.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body1.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body2 != null && body2.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body2.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body3 != null && body3.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body3.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body4 != null && body4.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body4.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body5 != null && body5.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body5.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body6 != null && body6.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body6.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body7 != null && body7.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body7.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body8 != null && body8.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body8.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body9 != null && body9.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body9.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body10 != null && body10.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body10.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body11 != null && body11.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body11.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body12 != null && body12.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body12.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body13 != null && body13.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body13.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }

        if (body14 != null && body14.activeSelf)
        {
            // Grab all Renderer components on this GameObject and its children
            Renderer[] renderers = body14.GetComponentsInChildren<Renderer>(includeInactive: false);

            if (renderers.Length > 0)
            {
                foreach (Renderer rend in renderers)
                {
                    rend.material = material;
                }
            }
        }


        
    }
}
